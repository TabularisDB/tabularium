apiVersion: v1
kind: Service
metadata:
  name: registry
  namespace: tabularis-registry
spec:
  type: ClusterIP
  selector:
    app: tabularis-registry
  ports:
    - name: http
      port: 80
      targetPort: 3000
